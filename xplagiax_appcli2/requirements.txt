# ════════════════════════════════════════════════════════════════════════════
#  xplagiax_appcli2 — requirements (imagen mínima de producción)
#
#  El stack ML pesado (tensorflow, torch, transformers, sentence-transformers,
#  scikit-learn, tokenizers) se EXCLUYE a propósito: el análisis de IA y la
#  búsqueda de fuentes se delegan a servicios externos (IA :5006, FinderX :8000).
#  Las funciones internas que dependían de esas libs (qdrant_search_fulltext con
#  sentence-transformers y el topic-classifier con sklearn) solo fallarán si esas
#  rutas concretas se invocan; el arranque de la app no las necesita (imports lazy).
# ════════════════════════════════════════════════════════════════════════════

# ── Web framework + extensiones ──────────────────────────────────────────────
Flask==3.1.1
Werkzeug==3.1.3
itsdangerous==2.2.0
Flask_Cors==5.0.0
Flask_Limiter==3.5.0
Flask_Login==0.6.3
flask_mail==0.10.0
Flask_Migrate==4.1.0
Flask_SocketIO==5.5.1
flask_sqlalchemy==3.1.1
Flask-WTF==1.2.1
WTForms==3.1.2
SQLAlchemy==2.0.38

# ── Driver de base de datos (MySQL, mysql+pymysql) — Python puro ─────────────
PyMySQL==1.1.1

# ── Autenticación / OAuth ────────────────────────────────────────────────────
Authlib==1.4.1
Flask-Dance==7.1.0
PyJWT==2.10.1
bcrypt==4.2.1

# ── Servidor WSGI de producción ──────────────────────────────────────────────
gunicorn==23.0.0

# ── Tareas programadas / cola ────────────────────────────────────────────────
APScheduler==3.10.4
celery==5.5.2

# ── Pagos ────────────────────────────────────────────────────────────────────
stripe==10.10.0
paypalrestsdk==1.13.3

# ── HTTP / utilidades ────────────────────────────────────────────────────────
Requests==2.32.4
urllib3==2.5.0
psutil==5.9.5
python-dotenv==1.0.1

# ── Cache / vectorial ────────────────────────────────────────────────────────
redis==5.2.1
qdrant_client==1.15.1

# ── Documentos e imágenes ────────────────────────────────────────────────────
PyMuPDF==1.24.10
Pillow==11.3.0
python-docx==1.1.2
reportlab==4.3.0

# ── NLP ligero / numérico ────────────────────────────────────────────────────
numpy==2.3.2
fast_langdetect==0.3.2
joblib==1.4.2

cryptography>=42.0

# ── Two-Factor Authentication (TOTP) ─────────────────────────────────────────
pyotp==2.10.0
qrcode==8.2
